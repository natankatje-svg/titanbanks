# HTTP Basic Auth for private preview deploys.
# Set BASIC_AUTH_ENABLED=false to disable (default is ON when these are present).
BASIC_AUTH_ENABLED=true
BASIC_AUTH_USER=titan
BASIC_AUTH_PASSWORD=change-me
